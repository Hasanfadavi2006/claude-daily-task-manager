# دستیار برنامه‌ریز روزانه (Daily Task Manager)

سند طراحی کامل — نسخه ۱

> هدف: یک سیستم شخصی که داده‌های روزانه‌ات را از منابع مختلف (روبیکا، پیامک، واتساپ، تلگرام، تقویم) جمع می‌کند، رویدادها را یک‌جا ضبط می‌کند، با کمک هوش مصنوعی اولویت‌بندی می‌کند و هر روز یک برنامه‌ی عملی به تو می‌دهد.

---

## ۱) چشم‌انداز و خروجی نهایی

صبح که بیدار می‌شوی، در تلگرام (یا هر مقصدی که انتخاب کنی) یک پیام داری:

```
📅 برنامه‌ی امروز — سه‌شنبه ۷ خرداد

🔴 فوری و مهم
  ۱. جواب پیام مدیر در گروه «پروژه آلفا» (روبیکا) — قبل از ۱۰ صبح
  ۲. تماس با مشتری درباره فاکتور (پیامک دیروز)

🟡 مهم ولی نه فوری
  ۳. مرور تسک‌های گروه «تیم فنی» — ۳ تسک معطل
  ۴. جلسه ساعت ۱۴ (از تقویم)

🟢 کم‌اولویت / در صورت وقت
  ۵. جواب پیام‌های شخصی واتساپ (۵ پیام)

🧠 خلاصه دیروز
  - ۳ تصمیم گرفته شد، ۲ تسک بسته شد، ۱ تسک به امروز موکول شد
```

همه‌چیز خودکار جمع و تحلیل می‌شود؛ تو فقط برنامه را می‌گیری و در صورت لزوم بازخورد می‌دهی.

---

## ۲) معماری کلی

سه لایه‌ی اصلی:

```
┌─────────────── منابع داده ───────────────┐
│  روبیکا   پیامک   واتساپ   تلگرام   تقویم  │
└───────┬───────┬──────┬───────┬───────┬────┘
        │       │      │       │       │
        ▼       ▼      ▼       ▼       ▼
┌──────────────────────────────────────────┐
│        لایه‌ی جمع‌آوری (Collectors)         │
│  هر منبع → تبدیل به «رویداد استاندارد»     │
└────────────────────┬─────────────────────┘
                     ▼
┌──────────────────────────────────────────┐
│            دیتابیس مرکزی (Storage)         │
│   events · tasks · daily_plans · contacts  │
└────────────────────┬─────────────────────┘
                     ▼
┌──────────────────────────────────────────┐
│         مغز / موتور تحلیل (Claude)         │
│  دسته‌بندی · اولویت‌بندی · برنامه‌ریزی روزانه │
└────────────────────┬─────────────────────┘
                     ▼
┌──────────────────────────────────────────┐
│            لایه‌ی خروجی (Delivery)         │
│      بات تلگرام → برنامه روزانه به گوشی     │
└──────────────────────────────────────────┘
```

ایده‌ی کلیدی: **هر منبع داده هرچقدر هم متفاوت باشد، در نهایت به یک قالب واحد «رویداد» تبدیل می‌شود.** بعد از آن، بقیه‌ی سیستم دیگر نمی‌داند داده از روبیکا آمده یا واتساپ — همه یکسان پردازش می‌شوند. این کار سیستم را تمیز و توسعه‌پذیر می‌کند.

---

## ۳) قالب استاندارد رویداد (Normalized Event)

قلب طراحی این است. هر پیام/رویداد از هر منبع به این شکل درمی‌آید:

```json
{
  "id": "uuid",
  "source": "rubika | sms | whatsapp | telegram | calendar",
  "source_id": "شناسه اصلی پیام در منبع",
  "chat_type": "private | group | channel",
  "chat_name": "پروژه آلفا",
  "sender": "نام/شماره فرستنده",
  "is_from_me": false,
  "content": "متن پیام",
  "attachments": ["نوع و لینک فایل‌ها در صورت وجود"],
  "timestamp": "2026-05-28T09:12:00+03:30",
  "raw": { "...داده خام اصلی برای مواقع نیاز..." }
}
```

و خروجی پردازش، «تسک» است:

```json
{
  "id": "uuid",
  "from_event": "uuid رویداد منبع",
  "title": "جواب پیام مدیر در گروه پروژه آلفا",
  "category": "work | personal | finance | meeting | ...",
  "priority": "urgent | high | medium | low",
  "due": "2026-05-28T10:00:00+03:30 یا null",
  "status": "open | done | snoozed | ignored",
  "reason": "چرا این اولویت داده شد (توضیح Claude)"
}
```

---

## ۴) تحلیل منبع‌به‌منبع (مهم‌ترین بخش)

تو اولویت را روی روبیکا، پیامک/واتساپ و تلگرام گذاشتی. واقعیت هر کدام متفاوت است:

### 🟦 روبیکا (گروه‌های شرکت) — اولویت اصلی تو
- **وضعیت:** روبیکا **API رسمی عمومی برای اکانت شخصی ندارد.**
- **گزینه A — کتابخانه‌ی غیررسمی (مثل `rubpy`):** با لاگین اکانت کاربری، پیام‌های گروه‌ها را می‌خواند.
  - مزیت: به همه‌ی گروه‌ها و پیام‌های شخصی‌ات دسترسی دارد.
  - ریسک: ❗ امکان مسدود شدن اکانت، شکستن کتابخانه با تغییر API روبیکا، و **مسئله‌ی محرمانگی شرکت** (پایین‌تر توضیح دادم).
- **گزینه B — بات رسمی روبیکا:** اگر روبیکا پلتفرم بات داشته باشد، یک بات بسازیم و در گروه‌های شرکت اضافه کنیم.
  - مزیت: رسمی، بدون ریسک بَن، پایدار.
  - شرط: باید بررسی کنیم که آیا بات می‌تواند پیام‌های گروه را بخواند و آیا ادمین گروه‌ها اجازه می‌دهد.
- **پیشنهاد من:** اول گزینه‌ی B (بات رسمی) را بررسی کنیم؛ اگر ممکن نبود، گزینه‌ی A با یک اکانت و آگاهی کامل از ریسک.

> ⚠️ هشدار مهم محرمانگی: داده‌ی گروه‌های شرکت متعلق به شرکت است. استخراج آن و فرستادنش به یک سرویس هوش مصنوعی بیرونی ممکن است **سیاست محرمانگی شرکت را نقض کند.** قبل از هر کاری باید این را روشن کنیم (بخش ۸).

### 💬 پیامک (SMS)
- **اندروید:** کاملاً شدنی.
  - گزینه‌ی ساده: یک اپ آماده‌ی «SMS Forwarder» که پیامک‌ها را به یک Webhook یا بات تلگرام POST می‌کند (بدون کدنویسی اپ).
  - گزینه‌ی حرفه‌ای: یک اپ کوچک اندروید با مجوز `READ_SMS` که مستقیم به سرور ما می‌فرستد.
- **آی‌اواس:** عملاً ممکن نیست (اپل اجازه‌ی خواندن SMS دلخواه را نمی‌دهد).
- **پیشنهاد:** شروع با اپ آماده‌ی SMS Forwarder → سریع و بی‌دردسر.

### 🟢 واتساپ
- **وضعیت:** واتساپ **API رسمی برای خواندن چت‌های شخصی ندارد.** فقط WhatsApp Business Cloud API وجود دارد که برای شماره‌های تجاری است، نه چت شخصی.
- **گزینه‌ی غیررسمی:** کتابخانه‌هایی مثل `Baileys` یا `whatsapp-web.js` که مثل واتساپ‌وب عمل می‌کنند.
  - ریسک: ❗❗ بالاترین ریسک بَن بین همه. واتساپ با این کتابخانه‌ها سختگیر است.
- **پیشنهاد:** اگر واقعاً لازم است، فقط با یک شماره‌ی فرعی و با پذیرش ریسک. وگرنه فعلاً به تعویق بیندازیم.

### ✈️ تلگرام
- **وضعیت:** بهترین وضعیت بین همه. ✅
  - **اکانت شخصی:** با `Telethon` یا `Pyrogram` (پروتکل رسمی MTProto). تلگرام رسماً از کلاینت‌های کاربری پشتیبانی می‌کند → ریسک بَن بسیار کم.
  - **بات:** با Bot API رسمی برای گروه‌ها.
- **پیشنهاد:** Telethon برای اکانت شخصی + بات رسمی برای ارسال برنامه‌ی روزانه.

### 📅 تقویم گوگل (پیشنهاد اضافه از من)
- من همین حالا مستقیم به آن وصل هستم (از طریق MCP). سریع‌ترین برد است و جلسات/قرارها را خودکار وارد برنامه می‌کند. توصیه می‌کنم در فاز اول واردش کنیم چون «مجانی» است.

### جمع‌بندی ریسک

| منبع | اتصال رسمی | ریسک | توصیه |
|------|:--:|:--:|------|
| تلگرام | ✅ | کم | فاز اول |
| تقویم گوگل | ✅ | هیچ | فاز اول |
| پیامک (اندروید) | نیمه | کم | فاز دوم |
| روبیکا | ❌ | متوسط | فاز دوم، با احتیاط |
| واتساپ | ❌ | زیاد | فاز سوم یا حذف |

---

## ۵) مدل داده (Database Schema)

شروع با **SQLite** (ساده، بدون نیاز به سرور)؛ بعداً در صورت رشد به **PostgreSQL**.

```sql
-- رویدادهای خام از همه‌ی منابع
CREATE TABLE events (
    id          TEXT PRIMARY KEY,
    source      TEXT NOT NULL,        -- rubika/sms/whatsapp/telegram/calendar
    source_id   TEXT,
    chat_type   TEXT,                 -- private/group/channel
    chat_name   TEXT,
    sender      TEXT,
    is_from_me  INTEGER DEFAULT 0,
    content     TEXT,
    timestamp   TEXT NOT NULL,
    raw         TEXT,                 -- JSON خام
    processed   INTEGER DEFAULT 0
);

-- تسک‌های استخراج‌شده توسط Claude
CREATE TABLE tasks (
    id          TEXT PRIMARY KEY,
    event_id    TEXT REFERENCES events(id),
    title       TEXT NOT NULL,
    category    TEXT,
    priority    TEXT,                 -- urgent/high/medium/low
    due         TEXT,
    status      TEXT DEFAULT 'open',  -- open/done/snoozed/ignored
    reason      TEXT,                 -- توضیح اولویت
    created_at  TEXT NOT NULL
);

-- برنامه‌ی روزانه‌ی تولیدشده
CREATE TABLE daily_plans (
    id          TEXT PRIMARY KEY,
    plan_date   TEXT NOT NULL,
    plan_json   TEXT NOT NULL,        -- ساختار کامل برنامه
    summary     TEXT,                 -- خلاصه‌ی دیروز
    generated_at TEXT NOT NULL
);

-- نگاشت مخاطبین/گروه‌ها برای تشخیص اهمیت
CREATE TABLE contacts (
    id          TEXT PRIMARY KEY,
    source      TEXT,
    identifier  TEXT,                 -- شماره/یوزرنیم/آیدی
    display_name TEXT,
    importance  TEXT DEFAULT 'normal' -- vip/normal/low
);
```

`contacts.importance` یک ترفند مهم است: مثلاً پیام مدیر همیشه VIP، پیام گروه‌های تبلیغاتی همیشه low.

---

## ۶) موتور تحلیل و برنامه‌ریزی (مغز)

این بخش جایی است که من (Claude) کار می‌کنم. سه مرحله:

1. **دسته‌بندی (Classification):** هر رویداد جدید → آیا اصلاً اکشن‌پذیر است؟ (خیلی پیام‌ها فقط نویز هستند). اگر بله → تبدیل به تسک با دسته و اولویت اولیه.
2. **اولویت‌بندی (Prioritization):** بر اساس فرستنده (VIP؟)، فوریت زمانی، کلمات کلیدی («فوری»، «امروز»، «ددلاین»)، و زمینه‌ی شغلی.
3. **برنامه‌ریزی (Planning):** تسک‌های باز + جلسات تقویم → یک برنامه‌ی روزانه‌ی منطقی با گروه‌بندی فوری/مهم/کم‌اولویت + خلاصه‌ی دیروز.

نمونه‌ی منطق فراخوانی Claude (شبه‌کد):

```python
prompt = f"""
این رویدادهای امروز کاربر است (از منابع مختلف):
{events_json}

تسک‌های باز قبلی:
{open_tasks_json}

جلسات تقویم امروز:
{calendar_json}

مخاطبین مهم (VIP): {vip_list}

یک برنامه‌ی روزانه‌ی عملی بساز:
- نویز را حذف کن
- بر اساس فوریت و اهمیت اولویت بده
- برای هر تسک دلیل کوتاه بنویس
- خلاصه‌ی کوتاهی از اتفاقات دیروز بده
خروجی را به صورت JSON با ساختار daily_plan بده.
"""
plan = claude.generate(prompt)   # با prompt caching برای صرفه‌جویی
```

> نکته: برای صرفه‌جویی در هزینه و سرعت، از **prompt caching** استفاده می‌کنیم (دستورها و زمینه‌ی ثابت کش می‌شوند).

---

## ۷) جریان روزانه (Daily Flow)

```
در طول روز:  Collectorها مدام رویدادها را در دیتابیس می‌ریزند
             ↓
هر شب/صبح:   Scheduler (cron یا APScheduler) موتور تحلیل را اجرا می‌کند
             ↓
             Claude برنامه‌ی فردا/امروز را می‌سازد
             ↓
             بات تلگرام برنامه را به گوشی‌ات می‌فرستد
             ↓
تعامل تو:     روی هر تسک دکمه می‌زنی: ✅ انجام شد / ⏰ بعداً / ❌ بی‌خیال
             ↓
             بازخورد در دیتابیس ذخیره و در برنامه‌ی بعدی لحاظ می‌شود
```

نکته‌ی «ضبط اتفاقات روزانه» که گفتی: همه‌ی رویدادها در جدول `events` می‌مانند و خلاصه‌ی روزانه در `daily_plans.summary` ساخته می‌شود — هر زمان بخواهی می‌توانی بپرسی «دیروز/هفته‌ی پیش چه گذشت؟»

---

## ۸) امنیت و حریم خصوصی (نباید نادیده گرفته شود)

این سیستم **همه‌ی پیام‌های شخصی و کاری‌ات را می‌خواند.** این یعنی سطح حساسیت بسیار بالا. اصول:

1. **میزبانی شخصی (Self-host):** سرور را خودت کنترل کن (VPS شخصی یا سرور خانگی)، نه سرویس شخص ثالث.
2. **رمزنگاری:** دیتابیس رمزنگاری‌شده در حالت ذخیره (at-rest)؛ کلیدها و توکن‌ها در فایل `.env` یا یک Vault، **نه** داخل کد.
3. **محرمانگی شرکت — مهم:** داده‌ی گروه‌های روبیکای شرکت متعلق به شرکت است. فرستادن آن به یک LLM بیرونی ممکن است سیاست‌های محرمانگی را نقض کند. گزینه‌ها:
   - اول از شرکت/مدیر اجازه بگیر.
   - یا فقط متادیتا (چه کسی، کِی، موضوع کلی) را پردازش کن، نه متن کامل.
   - یا برای داده‌ی حساس از یک مدل محلی (local LLM) استفاده کن تا داده از دستگاهت خارج نشود.
4. **کمینه‌سازی داده:** فقط چیزی که لازم است به Claude فرستاده شود؛ نویز قبلش فیلتر شود.
5. **حق فراموشی:** امکان حذف رویدادها بعد از N روز (مثلاً نگه‌داری ۳۰ روزه).

> این مورد را جدی می‌گیرم. قبل از وصل‌کردن داده‌ی شرکت، باید تکلیف بند ۳ روشن شود.

---

## ۹) پشته‌ی فناوری پیشنهادی (Tech Stack)

- **زبان:** پایتون ۳.۱۱+ (چون کتابخانه‌های تلگرام/روبیکا/SMS عمدتاً پایتونی‌اند).
- **دیتابیس:** SQLite در شروع → PostgreSQL در مقیاس.
- **کانکتورها:**
  - تلگرام: `Telethon`
  - روبیکا: بات رسمی (ترجیح) یا `rubpy`
  - پیامک: اپ SMS Forwarder → Webhook (FastAPI)
  - واتساپ: `Baileys` (Node) در صورت نیاز
  - تقویم: همین MCP که دارم
- **وب/Webhook:** `FastAPI`
- **زمان‌بندی:** `APScheduler` یا cron
- **هوش مصنوعی:** Anthropic SDK (Claude) با prompt caching
- **خروجی:** بات تلگرام (`python-telegram-bot`)
- **استقرار:** Docker روی یک VPS شخصی

---

## ۱۰) نقشه‌ی راه فازبندی‌شده (Roadmap)

### فاز ۰ — پایه
- اسکلت پروژه، دیتابیس، قالب رویداد استاندارد، کانفیگ امن (`.env`)

### فاز ۱ — اولین برد سریع
- کانکتور تلگرام (Telethon) + تقویم گوگل
- موتور اولویت‌بندی پایه با Claude
- بات تلگرام برای ارسال برنامه‌ی روزانه
- ✅ در این نقطه یک سیستم کارِ واقعی داری

### فاز ۲ — منابع اصلی تو
- کانکتور پیامک (SMS Forwarder)
- کانکتور روبیکا (اول بررسی بات رسمی)
- سیستم بازخورد (انجام شد / بعداً / بی‌خیال)

### فاز ۳ — تکمیل
- واتساپ (در صورت تصمیم به ریسک)
- خلاصه‌ی هوشمند دیروز/هفته
- یادگیری از بازخورد برای اولویت‌بندی بهتر

---

## ۱۱) تصمیم‌هایی که باید بگیری

قبل از شروع کدزنی، این‌ها را روشن کن:

1. **محرمانگی شرکت:** آیا اجازه‌ی استخراج داده‌ی گروه‌های روبیکای شرکت را داری؟ (مهم‌ترین مورد)
2. **روبیکا:** بات رسمی یا کتابخانه‌ی غیررسمی با ریسک بَن؟
3. **واتساپ:** ارزش ریسک بَن را دارد یا فعلاً کنار بگذاریم؟
4. **میزبانی:** VPS شخصی داری یا باید راه‌اندازی کنیم؟
5. **گوشی:** اندروید است یا آی‌اواس؟ (روی SMS و واتساپ اثر دارد)
6. **مدل پردازش:** برای داده‌ی حساس، Claude (ابری) قابل‌قبول است یا مدل محلی لازم داری؟

---

## خلاصه

این سیستم کاملاً شدنی است، اما یک «اپ» ساده نیست؛ یک سیستم چندبخشی است که قلبش یک **بات تلگرام + دیتابیس + موتور هوش مصنوعی** است. راحت‌ترین و امن‌ترین مسیر شروع: **تلگرام + تقویم** (فاز ۱)، بعد اضافه‌کردن **پیامک و روبیکا** (فاز ۲). واتساپ و داده‌ی شرکت بیشترین ریسک/حساسیت را دارند و باید با احتیاط و اجازه پیش بروند.
